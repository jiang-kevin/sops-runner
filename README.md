# sops-runner
Minimal runner image for SOPS secret management in CI/CD or other deployment workflows.

## Usage
This image is intended for pre-deploy or CI steps where you need the `sops` CLI available.
